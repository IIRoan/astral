[Unit]
Wants=chezmoi-init.service
After=chezmoi-init.service

[Service]
# Run the migration in Niri's own start transaction as a fallback for existing
# users whose global user presets have not yet been refreshed after a rebase.
ExecStartPre=/usr/libexec/astral/chezmoi-init
