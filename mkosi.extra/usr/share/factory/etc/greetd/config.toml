[general]
service = "greetd-spawn"

[terminal]
# Note: KMSCON will break greetd wherever the VT is started
vt = 1

[default_session]
command = "env NOCTALIA_GREETER_BIN=/usr/libexec/astral/noctalia-greeter-guard /usr/bin/noctalia-greeter-session -- --session Niri"
user = "greeter"
